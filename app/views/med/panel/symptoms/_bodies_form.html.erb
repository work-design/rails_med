<%= form_with scope: :symptom, model: @symptom, url: panel_symptom_path(@symptom) do |f| %>
  <%= f.collection_select :body_ids, @bodies, :id, :name, { wrap: { select: 'field-body control' } }, { multiple: true, data: { controller: 'choice' } } %>
  <%= f.submit %>
<% end %>
